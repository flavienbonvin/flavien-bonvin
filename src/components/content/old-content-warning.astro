---
import Divider from "@components/atoms/divider.astro";

interface Props {
    publicaitonDate: number;
}

const { publicaitonDate } = Astro.props;
const yearsSinceRelease = new Date().getFullYear() - publicaitonDate;
---

<blockquote>
    <p>
        This article is quite old (it was published {yearsSinceRelease} years ago). Its content might
        be outdated and not representing the current state of the industry or my view on it.
    </p>
</blockquote>

<Divider />
