---
import { PATHS } from "const";

const { pathname } = Astro.url;
const navLinks = [
    {
        href: PATHS.home,
        text: "Home",
    },
    {
        href: PATHS.dev,
        text: "Web dev",
    },
    {
        href: PATHS.insights,
        text: "Insights",
    },
    {
        href: PATHS.about,
        text: "About",
    },
];
---

<nav>
    <ul class="flex gap-4">
        {
            navLinks.map(({ href, text }) => (
                <li>
                    <a
                        class:list={[
                            "font-serif tracking-tight",
                            href === PATHS.dev &&
                                pathname.includes(href) &&
                                "font-bold underline decoration-dev",
                            href === PATHS.insights &&
                                pathname.includes(href) &&
                                "decoration-insights font-bold underline",
                            href === PATHS.about &&
                                pathname.includes(href) &&
                                "font-bold underline decoration-accent",
                        ]}
                        href={href}
                    >
                        {text}
                    </a>
                </li>
            ))
        }
    </ul>
</nav>
