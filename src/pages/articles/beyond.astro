---
import Layout from "@ui/layout/Layout.astro";
import PageHeader from "@ui/shared/PageHeader.astro";
import ArticleList from "@ui/article/ArticleList.astro";
import type { PageMeta } from "interface";

import { getBeyondArticles } from "@data/articles";

const beyondArticles = await getBeyondArticles();

const pageMeta: PageMeta = {
    title: "Beyond dev | Flavien Bonvin",
    description: "Life outside of coding. Discover my interest.",
};
---

<Layout pageMeta={pageMeta}>
    <PageHeader title="Explorations of a developer">
        {pageMeta.description}
    </PageHeader>
    <ArticleList articles={beyondArticles} />
</Layout>
